# Copy this file to `.streamlit/secrets.toml` for LOCAL DEVELOPMENT ONLY.
# For production (Streamlit Community Cloud), set the same values via
# the Streamlit Cloud dashboard → app settings → Secrets.
# DO NOT commit secrets.toml to git — it's already in .gitignore.

APP_PASSWORD = "change-me-to-your-team-password"
